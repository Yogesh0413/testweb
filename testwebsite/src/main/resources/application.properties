spring.application.name=testwebsite

# Server
server.port=8080

# H2 In-Memory Database
spring.datasource.url=jdbc:h2:mem:hoteldb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# H2 Console (accessible at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Admin credentials (local dev only ? override with env vars on Render)
admin.username=admin
admin.password=admin123

# Gmail SMTP (local dev)
# Use an App Password, NOT your real Gmail password.
# Generate one at: myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yyyyoooo.gesh@gmail.com
spring.mail.password=csen hmny hpzp eqlh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# The hotel's "from" address shown in emails
hotel.mail.from=yogi
hotel.mail.name=The Grand Horizon